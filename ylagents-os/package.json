{
  "name": "ylagents-os",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "bun run --cwd packages/spa dev",
    "build": "bun run --cwd packages/spa build"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
